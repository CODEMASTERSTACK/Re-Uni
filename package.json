{
  "name": "unidate-api",
  "version": "1.0.0",
  "description": "UniDate serverless API (Vercel) – custom token, OTP",
  "private": true,
  "engines": { "node": ">=18" },
  "dependencies": {
    "@clerk/backend": "^1.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0",
    "express": "^4.18.0",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.0"
  },
  "scripts": {
    "dev": "node api/local-server.js"
  }
}
